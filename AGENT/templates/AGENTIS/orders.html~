{% extends "base.html" %}
{% load staticfiles %}
	{% block title %} 
	<h2>
	 Добро пожаловать в личный кабинет!
	 </h2>
	{% endblock %}
{% block content %}
<div class="order">
	{% for order in o %}
	<div class="alien_off">
	{% if p.who == 2 %}
	<img src="/media{{order.img.img}}/" id="order_mess">		
	<div class="name_orders"><p>{{order.order_from.login}}</p></div>
	<img src="/media{{order.order_to.img.img}}/" id="order_off">	
	{% else %}
	<img src="/media{{order.img.img}}/" id="order_mess">	
	<p>{{order.order_to.login}}</p></div>
	<img src="/media{{order.order_to.img.img}}/" id="order_off">
	{% endif %}
	<div class="name_order"><p> {{order.href_order}}</p></div>
	<div class="price_orders"><p> Цена: {{order.price}}</p></div>
	</div>
	{% endfor %}
	</div>
</div>
{% endblock %}
